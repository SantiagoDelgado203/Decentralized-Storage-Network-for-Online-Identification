# =============================================================================
# Decentralized Storage Network - Environment Variables
# =============================================================================
# Copy this file to .env and customize for your deployment

# -----------------------------------------------------------------------------
# BOOTSTRAP NODE PEER ID (from bootstrap-identity/)
# This is the stable identity that all nodes connect to first
# -----------------------------------------------------------------------------
BOOTSTRAP_PEER_ID=QmaTPiRLg64y6wwYXybwsQLVUqqzqwpJSNQ8k5T5e6MyAG

# -----------------------------------------------------------------------------
# StorageNode Configuration
# -----------------------------------------------------------------------------

# P2P listening port (default: 11111)
DSN_PORT=11111

# DHT namespace - must match across all nodes (default: dsn)
DSN_NAMESPACE=dsn

# Data directory for ID.json and Bootstrap.txt (default: .)
DSN_DATA_DIR=/app/data

# Bootstrap peers - comma-separated list of multiaddrs
# For Docker (using service names):
DSN_BOOTSTRAP_PEERS=/dns4/storage-bootstrap/tcp/11111/p2p/${BOOTSTRAP_PEER_ID}

# For local development (using IP addresses):
# DSN_BOOTSTRAP_PEERS=/ip4/192.168.1.100/tcp/11111/p2p/${BOOTSTRAP_PEER_ID}

# External addresses to announce (for Docker/NAT)
# This tells other nodes how to reach this node from outside
# DSN_ANNOUNCE_ADDRESSES=/dns4/my-node.example.com/tcp/11111

# -----------------------------------------------------------------------------
# AdminNode Configuration
# -----------------------------------------------------------------------------

# API server port
ADMIN_API_PORT=5000

# P2P ports for admin node
ADMIN_P2P_PORT=4001
ADMIN_P2P_WS_PORT=4002

# CORS allowed origins (comma-separated)
ADMIN_CORS_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# Web Portal Configuration
# -----------------------------------------------------------------------------

# AdminNode API URL
NEXT_PUBLIC_API_URL=http://localhost:5000

# For production:
# NEXT_PUBLIC_API_URL=http://admin-node:5000
